rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 'books' 폴더 내의 사용자 별 폴더는 본인만 접근 가능
    match /books/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
